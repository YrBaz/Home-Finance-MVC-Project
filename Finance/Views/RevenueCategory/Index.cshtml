@model IEnumerable<Finance.Models.RevenueCategory>

@{
    ViewData["Title"] = "Index";
}

<div class="container p-3 text-white-50">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary text-white-50">Income Categories</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="RevenueCategory" asp-action="Create" class="btn btn-success"><i class="bi bi-plus-square"></i> &nbsp Add Category</a>
        </div>
    </div>
</div>
    <br />
@if(Model.Count() > 0)
    {
        <table class="table table-striped text-white-50" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Income Categories
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach(var revenuecategory in Model)
                {
                <tr>
                    <td width="25%">@revenuecategory.Name</td>
                    <td width="25%">
                        <div class="w-50 btn-group" role="group">
                            <a asp-controller="RevenueCategory" asp-action="Edit" asp-route-Id="@revenuecategory.Id" class="btn btn-primary mx-1"><i class="bi bi-pencil-square"></i></a>
                            <a asp-controller="RevenueCategory" asp-action="Delete" asp-route-Id="@revenuecategory.Id" class="btn btn-danger mx-1"><i class="bi bi-trash"></i></a>
                        </div>
                    </td>
                </tr>
                }
            </tbody>
            
        </table>
    }
    else
    {
        <p>No categories created yet</p>
    }