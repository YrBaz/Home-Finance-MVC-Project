@model IEnumerable<Finance.Models.Revenue>

@{
    ViewData["Title"] = "Index";
}

<div class="container p-3 text-white-50">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary text-white-50">Incomes</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="Revenue" asp-action="Create" class="btn btn-success"><i class="bi bi-plus-square"></i> &nbsp Add Income</a>
        </div>
    </div>
    <br />

    @if(Model.Count() > 0)
    {
        <table class="table table-striped text-white-50" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Income
                    </th>
                    <th>
                        Value
                    </th>
                    <th>
                        Category
                    </th>
                    <th>
                        Date
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach(var revenue in Model)
                {
                <tr>
                    <td width="25%">@revenue.Name</td>
                    <td width="25%">@revenue.Value</td>
                    <td width="25%">@revenue.RevenueCategory.Name</td>
                    <td width="25%">@revenue.CreatedDate</td>
                    <td width="25%">
                        <div class="w-50 btn-group" role="group">
                            <a asp-controller="Revenue" asp-action="Edit" asp-route-Id="@revenue.Id" class="btn btn-primary mx-1"><i class="bi bi-pencil-square"></i></a>
                            <a asp-controller="Revenue" asp-action="Delete" asp-route-Id="@revenue.Id" class="btn btn-danger mx-1"><i class="bi bi-trash"></i></a>
                        </div>
                    </td>
                </tr>
                }
            </tbody>
            
        </table> 
        <div class="container p-3">
            <p2>Total @Model.Sum(e => e.Value)</p2>
        </div>
    }
    else
    {
        <p>No items created yet</p>
    }

</div>